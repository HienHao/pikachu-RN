import {StyleSheet} from 'react-native';

const styleMainPlaySection = StyleSheet.create({
  mainSection: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
  },
});

export default styleMainPlaySection;
