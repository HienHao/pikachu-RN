import {StyleSheet} from 'react-native';

const styleTimebar = StyleSheet.create({
  main__timebar: {
    textAlign: 'center',
    fontSize: 15,
    marginLeft: 30,
    fontFamily: 'sans-serif-light',
    fontWeight: 'normal',
  },
});

export default styleTimebar;
